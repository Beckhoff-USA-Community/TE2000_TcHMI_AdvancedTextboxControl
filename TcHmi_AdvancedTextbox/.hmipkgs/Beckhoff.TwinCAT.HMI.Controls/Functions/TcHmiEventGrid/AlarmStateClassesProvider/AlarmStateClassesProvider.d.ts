declare module TcHmi {
    module Functions {
        module Beckhoff {
            function AlarmStateClassesProvider(rowValue: Server.Events.Event, dataIndex: number, rowNumber: number): string[];
        }
    }
}
